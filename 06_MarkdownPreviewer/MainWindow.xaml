<Window x:Class="MarkdownPreviewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarkdownPreviewer"
        mc:Ignorable="d"
        Title="Markdown Previewer" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Markdown Input -->
        <TextBox Grid.Column="0"
                 Text="{Binding MarkdownText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="14"
                 Margin="5"/>

        <!-- HTML Preview -->
        <DockPanel Grid.Column="1">
            <Button DockPanel.Dock="Top" 
                    Content="コピー" 
                    Command="{Binding CopyCommand}" 
                    Margin="5,5,5,0"/>
            <WebBrowser local:WebBrowserHelper.Html="{Binding HtmlText}"
                        Margin="5" />
        </DockPanel>
    </Grid>
</Window>
